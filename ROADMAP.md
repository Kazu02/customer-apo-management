# Roadmap

## Current

- **2026-08-03: Web App が 403 になる障害が発生し、同日復旧した。** 原因はオーナー（s***e@gmail.com）ではない編集者アカウント（3***e@gmail.com）で既存 Web App を再デプロイしたこと。`executeAs: USER_DEPLOYING` のため実行ユーザーが編集者側へ移り、その account が本 script のスコープを承認していないため匿名アクセスが全て 403 になった。version 20 へ戻しただけでは実行ユーザーが戻らず復旧しない。復旧手順はオーナー account の clasp 認証で同じ deployment を version 20 へ再デプロイすること（コード変更なし）。復旧後 `getCustomers` は 190件・ID重複0で HTTP 200。
- **2026-08-04: 親フォルダ `株式会社市場作り_ALL` のリンク共有（リンクを知っている全員＝編集者）は現状維持。** 3s3.cube の編集者権限は個別付与ではなくこの共有リンク経由であることが判明し、いったん完全解除したが、ユーザー判断で同日中に元どおり復元した（`anyoneWithLink` / `writer` / `allowFileDiscovery=false`・配下も未反映0件）。配下には顧客データ・振込先の口座情報・アフィリエイト管理・代理店スプシ・Apps Script 2本・実名入り個人スクショ約50点が含まれる。顧客フォームはオーナー実行のためどちらの状態でも無影響。
- **上記の結果、誤アカウントでの再デプロイ事故は再発しうる。** 唯一の防波堤はデプロイ前の `clasp show-authorized-user` によるオーナー目視確認。対策する場合の選択肢は DECISIONS.md 2026-08-04 を参照（編集者→閲覧者へ降格 / 個別共有へ切替 / 現状維持）。
- **2026-08-04: テスト顧客3件（ID 1 / 3 / 13・いずれも `須川一輝` 名義）を削除**。関連する `アポ報告` 1行・`総合_須川一輝` 3行も削除（計7行）。本番は190→188件（途中で新規1件が登録されたため191からの減）。バックアップは `C:\Users\shinh\Backups\market-customer-sheet\20260804-before-cleanup\`。
- **2026-08-05: 上記の削除が顧客IDの重複を引き起こしていたのを発見し、同日修正した。** `registerCustomer` がIDを行数（`getLastRow()`）で採番していたため、3行削除後の新規登録が既存IDを再利用し、ID 189 / 190 / 191 が別人同士で重複していた。重複ペアの後発1件はフォームから開けず、更新すると先発の別人を上書きする状態だった。採番を `nextCustomerId_()`（既存ID最大+1）へ修正し、後発3行へ 200 / 201 / 202 を振り直し、`アポ報告` の該当ラベル3行も完全一致のみ付け替えた。**version 34 として本番デプロイ済み**。実測で199行・distinct 199・重複0・欠番 1/3/13。詳細は DECISIONS.md 2026-08-05。
- **未デプロイの差分は解消済み（2026-08-05）**: `getSalesStaff` アクションはコードから削除し、HEAD＝本番配信内容（version 34）が一致した状態に戻した。理由は DECISIONS.md 2026-08-05 を参照。
- 顧客選択を「顧客名で検索」方式に刷新（2026-07-26）。GAS は version 20 に再デプロイ（URL維持）、GitHub Pages も更新済み。本番の140件で検索・選択・フォーム流し込みまで確認済み。

- 営業担当フルネーム統一はGAS本番反映・既存データ変換・GitHub Pages反映まで完了。
- 統合顧客管理は営業担当正規化後に再生成済み。本番スプレッドシートで `統合顧客管理` 198行・27列、`名寄せ` 148行を確認。GAS最終デプロイは version 17。
- 営業名簿 `SALES_STAFF` に `藤井勇大` を追加し計10名（2026-07-24）。GAS は version 19 に再デプロイ（URL維持）、GitHub Pages も更新。市場作り配下3プロジェクトで名簿一致。

## Next

- 統合（名寄せ）の本番動作確認: `統合顧客管理` / `名寄せ` シートが生成され、案件一覧が正しく表示されるか。
- 名寄せの手動修正フロー（未一致の `紐づけ顧客ID` 入力 → 再実行）の運用確認。

## Completed

- 既存顧客の選択を顧客名検索（コンボボックス）に変更。名前・会社名・ID・営業担当で絞り込み、ハイライト表示・キーボード操作・選択解除に対応。`getCustomerListData` に `company`/`name`/`staff` を追加。GAS version 20・GitHub Pages 反映済み（2026-07-26）。
- 既存顧客の年齢（全角数字）が再送信で消える不具合を修正（`fillCustomerForm` で半角正規化）（2026-07-26）。
- 営業担当名簿を `SALES_STAFF` 配列（main.js / index.html）に集約し `江口裕人` を追加（計9名）。選択肢は名簿から動的生成、正規化辞書も名簿から自動生成。GAS version 18 デプロイ・GitHub Pages 反映済み（2026-07-16）。
- 営業担当をアフィリンク紹介者と同じフルネーム選択式に変更。既存データは `顧客情報` / `アポ報告` ともフルネームへ正規化済み。
- 営業担当正規化後に `統合顧客管理` / `名寄せ` を再生成し、既存Web App URLを維持してGAS version 17へ最終デプロイ済み。
- おみくじを5段階 select（大大吉・大吉・中吉・小吉・凶）に変更（`index.html`）。
- 自己採点（10点満点）をアポ情報に追加し、`アポ報告` シートへ記録（`index.html` / `main.js`）。既存シートには `ensureHeaders` で列を補完。
- アフィリンク顧客管理 SS との名前ベース名寄せ統合（`main.js`）。`統合顧客管理` 出力シート、`名寄せ` 手動修正シート（候補提案にレーベンシュタイン距離）、片側のみ顧客の区別表示。
- 引き継ぎ用ドキュメント（PROJECT_CONTEXT / DECISIONS / ROADMAP）を実態に整備。

## Blockers

- なし（2026-08-03 の Web App 403 障害は同日中に復旧済み。下記「2026-08-03」の項を参照）。

## Backlog

- **`getCustomers` が無認証で顧客199件の氏名・会社名・担当営業を返している**（`ANYONE_ANONYMOUS`。`index.html` のパスワードはクライアント側のみでAPIを保護していない）。共有シークレット必須化などの対策を検討する。**Driveのリンク共有（編集者）も現存するため、露出経路は API と Drive の2本ある。**
- `総合_須川一輝` シート（ヘッダーのみ・退任者）の削除要否。
- 誤操作で残った deployment `AKfycbyhMFGNmQ4g…`(@22) の削除。**保留理由だった「version 22 の要否判断」は解消済み**（version 22 の中身＝`getSalesStaff` はコードから削除し、本番は version 34）。削除して差し支えない状態。
- `自己採点` の集計・可視化（営業別平均など）。
- 名寄せの自動マッチ精度向上（電話番号など追加キーでの照合）。
- `main.js` 等のコードを Git 追跡化（現状未追跡）。
